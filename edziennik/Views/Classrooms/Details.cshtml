@model edziennik.Models.ViewModels.ClassroomDetailsViewModel

@{
    ViewBag.Title = "Szczegóły";
}

<h2>Szczegóły</h2>

<div>
    <h4>Sala</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.SubjectsCount)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SubjectsCount)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Lista przedmiotów", "ClassroomSubjects","Subjects", new { classroomId = Model.Id }, new { @class = "btn btn-info" })
    @Html.ActionLink("Edycja", "Edit", new { id = Model.Id }, new { @class = "btn btn-warning" })
    @Html.ActionLink("Powrót do listy", "Index", null, new { @class = "btn btn-primary" })
</p>
